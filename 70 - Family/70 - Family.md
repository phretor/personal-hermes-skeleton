> **Note**: anonymized sample. Real names, birthdays, addresses, and
> Drive URLs replaced with placeholders. The structure (family-member
> wikilinks, parallel PARA layout under this subtree) matches what I
> actually use. See [README.md](../README.md) for context.

# Family

[[Your Name]]'s family-related files: finance, generic paperwork, duties, things to do or keep track of because of regulations and household life.

The family is composed of:

- [[Your Name]] (role, born `YYYY-MM-DD`)
- [[Partner Name]] (role, born `YYYY-MM-DD`)
- [[Child 1]] (role, born `YYYY-MM-DD`)
- [[Child 2]] (role, born `YYYY-MM-DD`)

Each family member has their own page under `[[60 - Entities]]/people/`.

This subtree uses a **nested PARA layout** so the same conventions
(MEMORY.md, lifecycle rules) apply inside it:

- `00 - Trust - Backup Plan/`: family trust, asset inventory, emergency
  contacts, guardianship plans.
- `10 - Projects/`: family-internal projects (e.g., a milestone birthday
  trip).
- `50 - Knowledge/`: recipes, household-specific knowledge.
- `60 - Admin/`: finance, health, properties, education, immigration
  documents.
- `80 - Traveling/`: itineraries, travel records.

See [[00 - Index]] for the rest of the vault.
